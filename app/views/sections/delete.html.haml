= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link')

.sections.delete
  %h2 Delete Section

  = form_for(:section, :url => {:action => 'destroy', :id => @section.id}) do |f|
    %p Are you sure that you want to permantly delete this section?
    %p.reference-name 
      = @section.name

    .form-button
      = submit_tag("Delete Section")
    
  
